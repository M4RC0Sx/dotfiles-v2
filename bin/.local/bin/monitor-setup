#!/bin/bash

#if [ "$(autorandr --current)" == "mobile" ]; then
#    autorandr --change dual-right
#else
#    autorandr --change mobile
#fi

sleep 2
# bspc wm -r
notify-send "Monitor setup changed!"
~/.config/bspwm/multimonitor-setup
~/.config/polybar/launch.sh
